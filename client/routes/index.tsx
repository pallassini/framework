import { Fragment } from "../../core/client/runtime";

export default function Home() {
	return (
		<Fragment>
			<h1 s="fw-title">Home</h1>
			<p>Router file-based: questa è <code>/</code>.</p>
		</Fragment>
	);
}
