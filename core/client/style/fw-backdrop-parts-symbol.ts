/**
 * Metadato opzionale su oggetti `Properties`: stringhe unite in ordine in
 * `backdrop-filter` / `WebkitBackdropFilter` da `resolveClasses`.
 */
export const FW_BACKDROP_PARTS = Symbol.for("fw.style.backdropParts");
