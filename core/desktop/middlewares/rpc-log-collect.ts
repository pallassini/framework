import type { DesktopContext } from "../routes/context";

/**
 * Contributi RPC dai middleware desktop (timeout ecc.): espandi quando un mw espone metadati su `ctx`.
 */
export function collectDesktopRpcMiddlewareLogParts(_ctx: DesktopContext): string {
	return "";
}
