/** Nome evento: main (Electrobun) → pagina client dopo hot-reload schema `db/`. */
export const FW_DB_SCHEMA_RELOAD_EVENT = "fw-db-schema-reloaded";

/** Dev (Vite HMR): file sotto la directory dati fwdb cambiati → refetch devtools / UI. */
export const FW_DB_DATA_CHANGED_EVENT = "fw-db-data-changed";
